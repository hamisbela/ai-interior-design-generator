{
  "name": "image-generator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build-blog-dev.js && vite",
    "build": "cp blogposts.csv public/ || true && node build-blog.js && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "together-ai": "*",
    "csv-parse": "^5.5.3",
    "marked": "^11.1.1",
    "fs-extra": "^11.2.0",
    "jsdom": "^22.1.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
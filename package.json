{
  "name": "image-generator",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node build-blog-dev.js && vite",
    "build": "node build-blog.js && cp public/_redirects dist/ && vite build"
  },
  "dependencies": {
    "together-ai": "*",
    "csv-parse": "^5.5.3",
    "marked": "^11.1.1",
    "fs-extra": "^11.2.0",
    "jsdom": "^22.1.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}